# Neuroscore

Applicazione client-side con TanStack Router e build statica Vite.

## Sviluppo locale

```bash
npm install
npm run dev
```

Apri `http://localhost:3000`.

## Build

```bash
npm build
```

L'output statico per il deploy e in `dist/`.
